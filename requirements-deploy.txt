# Extra deps used ONLY in the docker-compose (split) deployment, where the
# Furix services talk over real Kafka/Valkey/ClickHouse. The lite/in-process
# mode never imports these (the bus/cache/timeline have in-memory fallbacks).
kafka-python==2.0.2          # C5 bus adapter (containers/c5_bus.py _KafkaBus)
redis==5.2.1                 # C13 Valkey client (containers/c13_valkey.py _RedisCache)
clickhouse-connect==0.8.11   # C10 timeline client (containers/c10_clickhouse.py)
